import React, {memo} from 'react'
import {Text, View} from 'react-native'

const EventListItem: React.FC<EventModel> = memo(event => {
  return (
    <View>
      <Text>123</Text>
    </View>
  )
})

export default EventListItem
